# dsh-browser-use bundle patch: inserts the browser-control plugin row into
# the web profile roster. Applied as a profile bundle layer (the
# `dsh.bundle.patch` manifest field).
- insert:
    - id: browser-use
      name: 'dsh-browser-use'
